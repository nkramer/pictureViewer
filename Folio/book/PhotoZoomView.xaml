<UserControl x:Class="Folio.Book.PhotoZoomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:book="clr-namespace:Folio.Book"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Black">
    <Border x:Name="transformHolder">
        <Border.RenderTransform>
            <TransformGroup>
                <ScaleTransform ScaleX="1" ScaleY="1" />
                <TranslateTransform X="0" Y="0" />
            </TransformGroup>
        </Border.RenderTransform>
    <Grid x:Name="rootContainer" ClipToBounds="True">
        <!-- Current page view -->
        <book:PhotoPageView x:Name="currentPageView"/>

        <!-- Next page view (for page transitions) -->
        <!--<book:PhotoPageView x:Name="nextPageView" Visibility="Collapsed">
            <book:PhotoPageView.RenderTransform>
                <TranslateTransform X="0" Y="0" />
            </book:PhotoPageView.RenderTransform>
        </book:PhotoPageView>-->
    </Grid>
    </Border>
</UserControl>
